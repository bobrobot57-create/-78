# Настройка платёжных систем (FreeKassa + Cryptomus)

## Общая схема

1. **Бот** генерирует ссылку на оплату (FreeKassa) или создаёт инвойс (Cryptomus).
2. **Пользователь** переходит по ссылке и оплачивает.
3. **Платёжная система** отправляет webhook на ваш сервер.
4. **Сервер** проверяет подпись, создаёт код, отправляет его пользователю в Telegram.

---

## FreeKassa

### Регистрация

1. Зарегистрируйтесь на [free-kassa.ru](https://www.free-kassa.ru/).
2. Создайте магазин. Получите:
   - **ID магазина** (Merchant ID)
   - **Секретное слово 1** — для генерации ссылки
   - **Секретное слово 2** — для проверки webhook

### Настройка webhook

В личном кабинете FreeKassa → настройки магазина:

- **URL оповещения:** `https://ВАШ-ДОМЕН/payment/freekassa` (например `https://78-production.up.railway.app/payment/freekassa`)
- **Метод:** POST

### Настройка в боте

В админ-боте: **⚙️ Настройки** → **💳 FreeKassa** → отправьте:
```
merchant_id secret1 secret2
```

Или через переменные окружения:
```
FK_MERCHANT_ID=12345
FK_SECRET_1=ваше_секретное_слово_1
FK_SECRET_2=ваше_секретное_слово_2
FK_CURRENCY=USD
```

---

## Cryptomus

### Регистрация

1. Зарегистрируйтесь на [cryptomus.com](https://cryptomus.com/).
2. В личном кабинете получите:
   - **Merchant UUID**
   - **API ключ** (Payment API Key)

### Настройка webhook

При создании инвойса бот передаёт `url_callback` автоматически:
`https://ВАШ-ДОМЕН/payment/cryptomus` (например `https://78-production.up.railway.app/payment/cryptomus`)

Убедитесь, что `WEBHOOK_BASE_URL` задан в переменных окружения.

### Настройка в боте

В админ-боте: **⚙️ Настройки** → **₿ Cryptomus** → отправьте:
```
merchant_uuid api_key
```

Или через переменные окружения:
```
CRYPTOMUS_MERCHANT=uuid-вашего-мерчанта
CRYPTOMUS_API_KEY=ваш_api_ключ
```

---

## Цены

Цены задаются в **⚙️ Настройки** → **💵 Цены**:
```
15 25 35
```
(30 дней, 60 дней, 90 дней в USD)

---

## Связь клиентского и админ-бота

При каждой оплате:
- **Клиент** получает ключ в клиентском боте (@NeuralVoiceLabBot).
- **Владелец и админы** получают уведомление в админ-бот: кто оплатил, сумма, дни, выданный код.
- **Логи платежей** доступны в админ-боте: 📜 Логи платежей.

---

## Проверка

1. Запустите сервер с `WEBHOOK_BASE_URL`.
2. Настройте FreeKassa и/или Cryptomus.
3. В клиентском боте нажмите **🛒 Купить подписку**.
4. Должны появиться кнопки оплаты.
5. После тестовой оплаты ключ придёт пользователю автоматически.
